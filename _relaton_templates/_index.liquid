<!DOCTYPE html>
<html lang="{{ page.lang | default: site.lang | default: "en" }}">

  {% render 'head' %}

  <body>
    {% render 'header' %}
    <main>
      {% render 'document' for documents %}
    </main>

    {% render 'footer' %}

    {% render 'script' %}
  </body>

</html>
