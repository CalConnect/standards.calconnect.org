org: CalConnect/.metanorma

source: github
output_dir: _site/cc
file_routing: flat
cache_dir: .cache/aggregate
data_dir: _data

channels:
  - public

include_drafts: true

github:
  organizations:
    - CalConnect
  topic: metanorma-release
